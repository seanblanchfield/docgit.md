# Goal: Fix Spurious Draft State

When navigating to a file in view-only mode, it appears as a local draft. This is incorrect. The file should only be marked as a draft if the content is actually modified by the user.
